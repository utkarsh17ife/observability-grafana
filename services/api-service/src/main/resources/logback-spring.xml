<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Standard console appender with JSON output -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- These MDC keys are auto-populated by OpenTelemetry Java agent -->
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <includeMdcKeyName>trace_flags</includeMdcKeyName>
            <!-- Custom fields for service identification -->
            <customFields>{"service_name":"api-service"}</customFields>
        </encoder>
    </appender>

    <!-- Wrap with OpenTelemetry appender to ensure trace context is in MDC -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.mdc.v1_0.OpenTelemetryAppender">
        <appender-ref ref="CONSOLE"/>
    </appender>

    <root level="INFO">
        <appender-ref ref="OTEL"/>
    </root>

    <!-- Debug level for our application code -->
    <logger name="com.demo" level="DEBUG"/>

    <!-- Reduce noise from libraries -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.apache" level="WARN"/>
</configuration>
